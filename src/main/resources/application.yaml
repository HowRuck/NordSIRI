spring:
  threads:
    virtual:
      enabled: true
  application:
    name: SiriAnalyzer
  kafka:
    bootstrap-servers: localhost:9092

gtfs:
  fetch:
    interval-ms: 20000
  feed:
    url: https://realtime.gtfs.de/realtime-free.pb
