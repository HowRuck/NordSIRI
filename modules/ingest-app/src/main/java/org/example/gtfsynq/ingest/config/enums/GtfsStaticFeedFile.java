package org.example.gtfsynq.ingest.config.enums;

/**
 * Enum representing the static feed files in a GTFS feed
 */
public enum GtfsStaticFeedFile {
	TRIPS,
	STOPS,
	STOP_TIMES,
	ROUTES,
	FEED_INFO,
	CALENDAR_DATES,
	CALENDAR,
	ATTRIBUTIONS,
	AGENCY
}
